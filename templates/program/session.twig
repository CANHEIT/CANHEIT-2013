<!DOCTYPE html>
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <title>{{ name }} - CANHEIT 2013, Canada's premier Higher Education IT Conference</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

        <meta name="apple-itunes-app" content="app-id=595230973, app-argument=gb://guide/5396/event/{{ id }}">
        <meta name="google-play-app" content="app-id=com.guidebook.apps.CANHEIT2013.android">

        {% include 'head-bottom.html' %}
    </head>
    <body>
        <!--[if lt IE 9]>
            <p class="chromeframe">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> or <a href="http://www.google.com/chromeframe/?redirect=true">activate Google Chrome Frame</a> to improve your experience.</p>
        <![endif]-->

        {% include 'header.html' %}

          <article id="session">
            <header>
              {% include 'top-call-to-action.html' %}
              <a href="./#{{ startTime | date("l-g:iA") | lower }}" rel="home" class="return">{{ startTime | date("l") }}</a>
              <h1>{{ name }}</h1>
              
              <p>
                <span class="starttime">{{ startTime | date("D. M d") }} at {{ startTime | date("g:i A") }}</span>
                <span class="endtime">until {{ endTime | date("g:i A") }}</span>
                <span class="location">in {{ location }}</span>
              </p>
            </header>

            {% if video_url %}

            <div class="video">
              {% if 'hml.med.uottawa.ca' in video_url %}
                <iframe width="845" height="515" src="{{video_url}}" frameborder="0" scrolling="no"></iframe>
              {% endif %}
            </div>
            
            {% elseif should_have_video == "not_allowed" %}
            <aside class="video_{{should_have_video}}">
              Looking for the video of this session?
              <p class="note">Unfortunately, no video was taken of this session, at the request of the presenter.</p>
            </aside>
            {% elseif should_have_video == "cancelled" %}
            <aside class="video_{{should_have_video}}">
              Looking for the video of this session?
              <p class="note">Unfortunately, this session was cancelled.</p>
            </aside>
            {% elseif should_have_video == "workshop_so_no" %}
            <aside class="video_{{should_have_video}}">
              Looking for the video of this session?
              <p class="note">Unfortunately, no video was taken of this workshop. However, videos were recorded for most of the sessions given on <a href="./#monday">Monday</a>, <a href="./#tuesday">Tuesday</a> and <a href="./#wednesday">Wednesday</a>.</p>
            </aside>
            {% elseif should_have_video == "bof_so_no" %}
            <aside class="video_{{should_have_video}}">
              Looking for the video of this session?
              <p class="note">Unfortunately, <em>Birds of a feather</em> sessions were not recorded. However, videos were recorded for most of the other types of presentations given on <a href="./#monday">Monday</a>, <a href="./#tuesday">Tuesday</a> and <a href="./#wednesday">Wednesday</a>.</p>
            </aside>
            {% endif %}
            
            {% if links %}
            <h2>Links</h2>
            <ul id="links">
            {% for link in links %}
              {% if link.gb_url|slice(0, 4) == 'http' or link.gb_url|slice(0, 1) == '/' %}
              <li><a href="{{ link.gb_url }}">{{ link.title }}</a></li>
              {% endif %}
            {% endfor %}
            </ul>
            {% endif %}
            
            <h2>About the session</h2>

            {% if image %}

            <img src="{{ image }}" />

            {% endif %}

            <section id="description">{% autoescape false %}
              {{ description }}
            {% endautoescape %}
            </section>

					</article>
					{% include 'footer.html' %}

        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.2/jquery.min.js"></script>
        <script>window.jQuery || document.write('<script src="/js/vendor/jquery-1.8.2.min.js"><\/script>')</script>
        <script src="/js/vendor/jquery.fitvids.js"></script>
        <script src="/js/plugins.js"></script>
        <script src="/js/main.js"></script><script src="/js/vendor/jquery.smartbanner/jquery.smartbanner.js"></script>
        <script src="/js/vendor/jquery.collapse.js"></script>
        <script src="/js/vendor/jquery.cookie.js"></script>
        <script src="/js/vendor/meny.min.js"></script>
        <script src="/js/menu.js"></script>
        
        <script>
          $(document).ready(function(){
            // Target your .container, .wrapper, .post, etc.
            $(".video").fitVids({ customSelector: "iframe[src^='https://hml.med.uottawa.ca']"});
          });
        </script>


        <script>
            var _gaq=[['canheit._setAccount','UA-35890616-1'],['canheit._trackPageview']];
            (function(d,t){var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
            g.src=('https:'==location.protocol?'//ssl':'//www')+'.google-analytics.com/ga.js';
            s.parentNode.insertBefore(g,s)}(document,'script'));
        </script>
    </body>
</html>
